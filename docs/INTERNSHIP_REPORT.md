# 山东非遗可视化系统 · 实习报告 & 任务执行书

> **给 Agent 的阅读说明**：本文件是当前项目的最高执行指令。
> 开工前先读 `docs/HANDBOOK.md`（架构与规范）和 `docs/CONTRIBUTING.md`（Git 协作）。
> 每个任务都标注了：目标 / 改动文件 / 验收标准 / 依赖。照做即可，不需要再问"要干什么"。
> 有任何代码改动，必须按 `.dsh/skills/feishu-log.md` 的字段写开发日志。

---

## 0. 项目定位（一句话）

**山东省非物质文化遗产 WebGIS 可视化系统**：把山东省各级非遗项目（名录 + 传承人）以空间点位形式展示在真实地图上，支持按地市/类别/批次筛选、空间分析、图表统计与游览路线规划。

- 技术栈：Vue3 + Vite + TS + Pinia + Element Plus + **OpenLayers**（真实瓦片地图）+ ECharts + Node/Express 后端 + 天地图底图
- 与旧项目的关系：**继承数据与功能设计，重写技术实现**（旧项目是 C# WinForms + ArcEngine + WebView2 套皮，新项目纯 Web + 开源 GIS 引擎）

## 1. 现状盘点（已完成 ✅）

| 项 | 状态 |
|---|---|
| Vue3 脚手架 + 三层架构（data/services/views） | ✅ `src/` 已建 |
| MapAdapter 抽象 + OLMapAdapter，地图主页跑通 | ✅ OSM 占位底图 |
| 可折叠面板、顶部导航 5 页 | ✅ |
| 后端骨架（天地图代理、shp/excel 转换占位） | ✅ `server/` |
| Git 协作闭环（分支保护 + 防呆钩子 + PR 模板） | ✅ |
| 文档（README/DEV_PLAN/HANDBOOK/CONTRIBUTING/日志） | ✅ |

## 2. 资源盘点（旧仓库 `z985313021-alt/-` 可借用清单）

> 旧仓库关键路径：`LYF地图操作/23110908016王亮/WindowsFormsMap1/`

### 2.1 数据资源（最有价值，直接借用）

| 资源 | 路径（旧仓库内） | 内容 | 用途 |
|---|---|---|---|
| 非遗名录空间点位 | `LYF地图操作/【小黄鸭】非物质文化遗产、传承人空间点位数据/国家级非物质文化遗产代表性项目名录.shp`（含 dbf/prj/shx/xml） | 国家级非遗项目**带坐标的点数据** | 新系统核心数据源（转 GeoJSON） |
| 传承人空间点位 | 同目录 `国家级非物质文化遗产代表性项目代表性传承人.shp` | 传承人带坐标点数据 | 独立图层 / 详情联动 |
| 山东地市边界 | `VisualWeb/data/shandong.json` | 16 地市 GeoJSON（含 code/pinyin/center） | 行政热力、按市聚合 |
| 路网数据 | `VisualWeb/data/roads.json`（7.6MB） | 路网骨架 | 游览路线规划 |
| 汇总统计 | `VisualWeb/data/data.json` | 186 项 / 16 市 / 10 类计数 | 图表初始化 + 校验 |
| 美食非遗专题 | `山东省arcgis处理数据及底图/山东省美食非遗分布专题图/shandong_heritage_food.csv` + icons/ | 美食非遗 CSV + 图标库 | 专题页（可选） |
| 非遗图片库 | `VisualWeb/images/`（163 张，10 个分类目录：传统戏剧33/美术28/民间文学19/技艺17/音乐17/舞蹈13/民俗12/体育游艺杂技11/曲艺8/医药5） | 分类实物图片 | 详情卡片、列表卡片配图 |
| 原始 GDB 备份 | `WindowsFormsMap1/Data/ShandongICH.gdb` 与 Backup/ | ArcGIS FileGDB 全量数据 | 数据校验 / 补充字段（需 ArcGIS 或 ogr 转出） |

### 2.2 功能设计蓝图（借用旧网页的交互设计）

旧网页 `VisualWeb/index.html`（WebView2 套皮）已实现的功能，全部值得继承：

| 旧功能 | 说明 | 新系统对应 |
|---|---|---|
| 功能导航（概览/分布/类别/搜索/路径） | 左栏功能切换 | 顶部导航 + 左栏 |
| 地图三模式 | 点位分布 / 行政热力(choropleth) / 密度热力(heatmap) | OpenLayers 矢量 + turf 重写 |
| 时空演变滑块 | 按批次（第一批~第五批）过滤动画 | 时间轴筛选（注意：旧项目批次字段质量差，见 2.3） |
| 详情卡片 + 点赞/评论 | 点选非遗显示详情，互动 | Element Plus Drawer + 后端存储 |
| 图表四件套 | 类别饼图 / 地市排行 / 申报趋势 / 人气 TOP5 | ECharts 保留 |
| 精准搜索 | 按名称搜索定位 | 搜索框 + 定位 |
| 路径规划 + 寻访行程单 | 点击起终点生成路线 | 后端 Dijkstra 或前端 turf 简化版 |
| 图片智能匹配 | 按 `分类文件夹/名称{1-5}.{jpg|png|...}` 顺序尝试 | 沿用该策略写个 `getImageCandidates()` |

### 2.3 经验教训（旧项目 Bug 日志 → 新系统开发红线）

| 旧坑 | 教训 | 新系统对策 |
|---|---|---|
| Shapefile 字段名截断（10 字符） | 字段名不可信 | 转 GeoJSON 时统一重命名字段（如 `name/city/category/batch`） |
| 空值 `.ToString()` 崩溃 | 防御性编程 | 所有属性读取加空值兜底 `?? ''` |
| 批次字段 80% 为空/格式乱（'1' vs '第一批'） | 数据质量差 | 先跑数据体检；批次统计若不可靠就改用类别/地市统计 |
| CSV Excel 乱码（GB2312 vs UTF-8） | 编码坑 | Web 端导出统一用 XLSX 或 UTF-8 BOM |
| 坐标系缺失/不匹配（HRESULT 0x80040358） | 坐标系合规 | 数据导入时校验并统一转 WGS84（EPSG:4326） |
| 启动时序竞态（数据没加载完就体检） | 时序管理 | 前端 `loading` 状态 + 按需加载 |
| 路径硬编码导致"我这能跑你那不能" | 可移植性 | 一切路径走后端配置/相对路径 |

## 3. 系统设计（草案，待团队确认后锁定）

### 3.1 页面结构（顶部导航 + 多页面）

| 页面 | 路由 | 核心内容 |
|---|---|---|
| 地图主页 | `/` | 全屏地图（主体）+ 左栏功能面板（可折叠）+ 右栏图表/详情面板（可折叠） |
| 数据管理 | `/data` | 数据上传（shp/geojson/excel）、数据体检报告、字段映射 |
| 空间分析 | `/analysis` | 量算/缓冲区/叠加统计/按位置选择 |
| 图表可视化 | `/chart` | 类别/地市/趋势/TOP5 + 地图联动 |
| 非遗详情 | `/heritage/:id` | 单项目详情页（图片/属性/传承人/位置） |
| 关于 | `/about` | 项目说明 |

### 3.2 地图主页布局（继承旧网页三栏 + 升级）

```
┌─────────────────────── 顶部导航 ───────────────────────┐
│ 地图主页 │ 数据管理 │ 空间分析 │ 图表 │ 关于            │
├──────────┬───────────────────────────────┬─────────────┤
│ 左栏(可折叠)│       OpenLayers 地图          │ 右栏(可折叠) │
│ ·功能切换  │   ·天地图底图(矢量/影像)        │ ·ECharts    │
│ ·地市筛选  │   ·点位/热力/行政热力模式        │  饼图/排行  │
│ ·类别筛选  │   ·时空批次滑块                 │ ·详情卡片   │
│ ·搜索      │   ·点击弹出详情                 │ ·寻访行程单 │
└──────────┴───────────────────────────────┴─────────────┘
```

## 4. 任务分解（Agent 直接执行）

> 执行顺序 = 编号顺序。每个任务完成后在 `docs/feishu-log.md` 写一条日志并 commit。
> 分支约定：每个任务开 `feature/<任务代号>` 分支 → PR 到 dev。

### T1 数据标准化（后端，最高优先）
- **目标**：把旧仓库 shp 数据转成标准 GeoJSON 并入库
- **改动**：`server/scripts/convert.mjs`（用 `shapefile` npm 包读 shp+dbf → GeoJSON）
- **输入**：旧仓库 `【小黄鸭】.../国家级非物质文化遗产代表性项目名录.shp` + 传承人.shp
- **输出**：`server/data/heritage.geojson`、`server/data/inheritors.geojson`（统一 WGS84，字段重命名为 `id/name/city/category/batch/level/desc/photo`，空值兜底）
- **验收**：能统计出 ≥180 个名录要素；字段无截断、无空值崩溃；坐标在山东范围（lng 114.8~122.7, lat 34.3~38.4）
- **依赖**：无（数据在旧仓库，需先 clone 或用本机文件）

### T2 底图接入（天地图）
- **目标**：替换 OSM 为天地图 WMTS（矢量/影像/注记）
- **改动**：`src/data/sources/tianditu.ts`（WMTS 实现）+ `server/` 代理接口 `/api/tianditu/:type`（tk 走后端 `.env`）
- **验收**：地图主页可切换矢量/影像底图且无偏移（3857 投影一致）
- **依赖**：用户提供 tk（`TIANDITU_TK` 填入 `server/.env`）；**未提供前保持 OSM 占位，不阻塞其他任务**

### T3 数据层接入（前端）
- **目标**：前端可加载 heritage.geojson 并渲染点位 + 图例
- **改动**：`src/data/sources/geojson.ts`、`src/services/map/OLMapAdapter.ts`（addVectorLayer 支持样式）、`src/services/stores/dataStore.ts`
- **验收**：地图主页显示非遗点位图层，按类别渲染不同颜色，点击弹详情
- **依赖**：T1

### T4 数据管理页 + 上传 + 体检
- **目标**：网页上传 shp/geojson/excel，出数据体检报告（字段/坐标/空值检查）
- **改动**：`src/views/DataManage.vue` + `server/index.js`（`/api/convert/*`、`/api/health-check`）
- **验收**：上传旧 shp 能转成图层显示；体检能报出"哪些字段缺失/哪些坐标异常"
- **依赖**：T1、T3

### T5 查询与筛选（核心交互）
- **目标**：按地市/类别/批次/关键词筛选地图与图表联动
- **改动**：`src/services/analysis/query.ts`、左栏筛选组件、ECharts 联动
- **验收**：选"济南+传统戏剧"→ 地图只剩对应点位，饼图/排行同步刷新
- **依赖**：T3

### T6 时空演变（批次滑块）
- **目标**：批次时间轴过滤点位，带动画
- **改动**：`src/components/map/TimeSlider.vue` + 查询服务扩展
- **注意**：先跑数据体检确认批次字段质量；质量差则降级为"按批次着色"而非统计
- **验收**：滑到"第三批"只显示该批次点位
- **依赖**：T5

### T7 空间分析三件套
- **目标**：量算（测距/测面）、缓冲区、叠加统计（区域内计数）
- **改动**：`src/services/analysis/measure.ts`（ol/sphere）、`buffer.ts`（turf）、`overlay.ts`（turf booleanPointInPolygon）、`src/views/Analysis.vue`
- **验收**：画线测距显示公里数；对某点位生成 50km 缓冲区并统计区内非遗数
- **依赖**：T3

### T8 详情页 + 图片库
- **目标**：非遗详情页（属性/图片/传承人/位置），图片走旧仓库 163 张图片库
- **改动**：`src/views/HeritageDetail.vue`、`src/services/image.ts`（沿用 `getPossibleImagePaths` 策略）
- **验收**：从地图点击 → 详情页显示字段 + 分类图片（有图）或占位图（无图）
- **依赖**：T5；图片库文件需拷入 `public/images/`（从旧仓库）

### T9 图表可视化页 + 地图联动
- **目标**：类别饼图/地市排行/申报趋势/人气 TOP5，点击图表定位地图
- **改动**：`src/components/chart/*.vue`（ECharts 封装）、`src/views/ChartView.vue`
- **验收**：点击地市排行某市 → 地图缩放到该市并高亮
- **依赖**：T5

### T10 游览路线规划（可选加分）
- **目标**：选起终点 → 生成非遗寻访路线 + 行程单
- **改动**：`server/` 路网 API（roads.json）+ `src/components/route/*`
- **验收**：两点间路线在地图上绘出，行程单列出沿途非遗
- **依赖**：T3；roads.json 7.6MB 放后端

### T11 Cesium 3D 演示（最后）
- **目标**：3D 地球 + 非遗点位 + 2D↔3D 切换
- **依赖**：全部完成后

## 5. 资源迁移动作（谁负责）
- [ ] clone 旧仓库 `z985313021-alt/-`（本机已有 blobless 副本在 temp，正式迁移需完整文件）
- [ ] 拷贝 `VisualWeb/images/` → 新项目 `public/images/`（163 张）
- [ ] 转换 2 个 shp → `server/data/*.geojson`（T1 产出）
- [ ] 拷贝 `shandong.json`、`roads.json` → 对应 data 目录
- [ ] 用 `data.json` 的数字做图表初值校验（186/16/10）

## 6. 分工建议（4~5 人实习队）

| 角色 | 负责任务 | 说明 |
|---|---|---|
| 架构/前端框架（组长） | T2、T5、全局状态 | 管地图与筛选联动 |
| 数据工程师 | T1、T4、资源迁移 | 数据质量是成败关键 |
| 可视化工程师 | T6、T9 | ECharts 与时间轴 |
| 空间分析工程师 | T7、T10 | turf 与路线 |
| 文档/测试 | 日志、验收、写报告 | 全程飞书留痕 |

## 7. 里程碑（4 周）

| 周 | 交付 |
|---|---|
| W1 | T1 数据 + T2 底图 + T3 点位渲染（**第一张能看的图**） |
| W2 | T4 上传体检 + T5 筛选联动 + T6 时空 |
| W3 | T7 分析 + T8 详情 + T9 图表 |
| W4 | T10 路线（可选）+ T11 Cesium + 报告定稿 + 验收演示 |

## 8. 风险清单

| 风险 | 等级 | 对策 |
|---|---|---|
| 旧 shp 数据缺失/损坏 | 高 | GDB 备份兜底；体检先行 |
| 批次字段质量差 | 中 | 降级为按类别/地市统计 |
| 天地图 tk 未申请 | 低 | OSM 占位，不阻塞 |
| 图片版权 | 中 | 仅内部实习使用，标注来源 |
| 路线规划性能 | 低 | roads.json 放后端，前端只画结果 |
