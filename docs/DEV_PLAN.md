# WebGIS 实习项目 — 开发事项记录

> 本文件是项目的"开发事项总账"，记录已确认决策、功能清单、排期、分工。
> 配套的本地技能手册在 `.dsh/skills/` 目录，开发前应先读取对应技能。
> 每次开发改动须同步在飞书开发日志（见 `.dsh/skills/feishu-log.md`）留痕。

---

## 0. 文档状态

- 创建日期：2025（调研阶段）
- 阶段：调研 + 脚手架搭建
- 负责人：实习小队（见分工流程）
- 关联仓库：待建（Git + GitHub）

---

## 1. 已确认技术决策（固化，勿随意改动）

| 项目 | 决策 | 说明 |
|---|---|---|
| 框架 | Vue3 + Vite + TypeScript | 实习项目主流，构建快 |
| 状态管理 | Pinia | Vue3 官方推荐，逻辑层核心 |
| UI 组件库 | Element Plus | 表格/表单/弹窗/折叠面板齐全 |
| 地图引擎(主) | OpenLayers | 2D，数据好找（shp/geojson/wms/excel） |
| 地图引擎(辅) | Cesium | 最后阶段，仅视觉演示，无城市模型/地形 |
| 底图 | 天地图（WMTS，需 tk） | 国内合规，矢量/影像/注记齐全 |
| 后端 | Node.js + Express（轻量） | 代理 tk、解析 shp/excel、WMS 中转 |
| 数据格式 | Shapefile / GeoJSON / WMS / Excel | 全支持 |
| 空间分析 | 量算+逆地理+属性查询+缓冲区+叠加统计+勾画 | 用户要求"都要有" |
| 飞书 | 开发工作日志（字段见技能手册） | 每次修改/调试/尝试/问题/创新都记 |
| 协作 | Git + GitHub（分支/PR/合并） | 团队开发流程 |

### 后端用途说明（用户曾不确定，特此记录）
1. 代理天地图 tk：前端直连会暴露密钥，后端中转保护。
2. Shapefile 解析：浏览器读不了 .shp/.dbf/.prj 多文件，后端转 GeoJSON。
3. Excel 解析：后端用库读 xlsx 转 GeoJSON（带经纬度的行）。
4. WMS 代理（可选）：跨域中转。
5. 开发日志中转（可选）：前端一键推飞书，建议先用 SDK/手动。

---

## 2. 功能清单（完整，定稿）

### A. 项目脚手架与分层
- [ ] `npm create vite@latest` 建 Vue3+TS 项目
- [ ] 安装依赖：ol / pinia / element-plus / axios / xlsx（前端）；express + shapefile + 等（后端）
- [ ] 目录分层（严格隔离三端）：
  ```
  src/
    data/        # 数据层：api 请求、数据源适配、仓储
    services/    # 逻辑层：地图业务、空间分析、Pinia stores
    views/       # 显示层：页面（多页面路由）
    components/  # 显示层：地图容器、控制面板、图表等
    router/      # 顶部导航路由
    config/      # 天地图/后端地址配置
  server/        # Node 后端
  docs/          # 实习计划书/记录/分工（也上传飞书）
  .dsh/skills/   # 本地技能手册
  ```
- [ ] 地图引擎抽象接口（OLMapAdapter / 以后 CesiumAdapter），显示层只依赖接口

### B. 页面与显示层（顶部导航 + 多页面 + 可折叠面板）
- [ ] 顶部导航栏：首页地图 / 数据管理 / 空间分析 / 图表可视化 / 关于
- [ ] 地图主页：全屏地图为绝对主体 + 可折叠侧边控制面板（图层开关、底图切换、绘制工具调出）
- [ ] 数据管理页：上传 shp/geojson/excel、WMS 接入表单、数据列表表格
- [ ] 空间分析页：量算/缓冲区/属性查询/叠加统计工具面板
- [ ] 图表可视化页：数据列表 + ECharts 图表，可折叠联动地图
- [ ] 所有面板支持折叠/按钮调出（"可选择是否展示"）

### C. 数据层
- [~] 天地图 WMTS 底图（矢量/影像已接入 + 后端代理 tk；注记待加；tk 填入 .env 即出图）
- [ ] GeoJSON 加载/编辑
- [ ] Shapefile 上传→后端转 GeoJSON
- [ ] Excel 上传→后端转带几何 GeoJSON
- [ ] WMS 服务接入与渲染
- [ ] 数据仓储（Pinia store 缓存已加载图层）

### D. 逻辑层（Pinia + 服务）
- [ ] 地图核心服务（初始化、图层注册、事件总线）
- [ ] 空间分析服务：距离/面积量算、逆地理编码、属性查询高亮、缓冲区、叠加统计
- [ ] 绘制/勾画服务（点线面绘制、编辑、导出）
- [ ] 图表联动服务（选中要素→图表过滤）

### E. 空间分析（用户要求全有）
- [ ] 测距、测面
- [ ] 坐标/逆地理查询
- [ ] 属性查询 + 高亮筛选
- [ ] 缓冲区生成
- [ ] 简单叠加统计（区域内计数等）
- [ ] 勾画/标注（后续详谈细化）

### F. 分工流程
- [ ] 角色：架构/脚手架、地图引擎、数据层、逻辑层、显示层/UI、后端、文档+飞书
- [ ] 里程碑排期（见 §3）

### G. 实习文档
- [ ] 实习计划书（目标/技术栈/分层架构/排期/分工）
- [ ] 实习记录（按飞书字段，每次开发留痕）
- [ ] 分工流程说明

### H. 飞书集成
- [ ] 飞书文档/多维表格按字段建模板
- [ ] 开发日志上传（手动或脚本，需飞书 app 凭证）

### I. Git / GitHub 协作
- [ ] 建仓库、分支策略（main/dev/feature-*）、PR 模板、.gitignore
- [ ] 提交规范（feat/fix/docs...）

### J. Cesium（最后阶段，暂定）
- [ ] 3D 地球 + 天地图影像演示、2D↔3D 切换页

---

## 3. 排期里程碑（草案，按周）

| 周次 | 阶段 | 关键交付 |
|---|---|---|
| W1 | 调研 + 脚手架 | Vue3 项目跑通、分层目录、天地图底图出图 |
| W2 | 数据层 | shp/geojson/excel/WMS 接入，数据管理页 |
| W3 | 逻辑层+空间分析 | 量算/缓冲区/查询/勾画服务 |
| W4 | 显示层+UI | 多页面、可折叠面板、图表联动 |
| W5 | 后端完善 + 联调 | 后端代理/解析稳定，前后端联调 |
| W6 | 文档+飞书+Git | 计划书/记录/分工定稿，飞书留痕，GitHub 协作跑通 |
| W7+ | Cesium 演示（可选） | 3D 视觉页 |

> 排期为草案，按实际进度滚动更新，更新须在飞书记录。

---

## 4. 待用户确认事项

1. **天地图 tk**：用户是否有天地图密钥（http://lbs.tianditu.gov.cn 免费申请）？无则先用占位符。
2. **飞书凭证**：自动/半自动上传需飞书自建应用 app_id + app_secret + 文档/多维表格 ID；否则先手动。
3. **Cesium 范围**：最后阶段再定，当前不展开。

---

## 5. 变更记录（本文件自身的修改留痕）

| 日期 | 修改人 | 改动摘要 |
|---|---|---|
| 创建日 | 架构 | 初版：决策表、功能清单、排期、待确认项 |
| 创建日+ | 架构 | 新建本地技能手册 `.dsh/skills/`（9 个技能）+ 搭建 Vue3 脚手架：package.json/vite/tsconfig/index.html/main.ts，分层目录 data/services/views/components/router，地图页跑通 OSM 占位底图 + 可折叠面板；后端 server/ 骨架（天地图代理+转换占位）；.gitignore/.env.example |
| 2026-08-31 | 架构 | 实现天地图 WMTS 接入：后端 `/api/tianditu/:type` 代理（tk 回源 + 状态接口）、前端 WMTS 源 + OSM 兜底自动降级；本地环境补装 Node/依赖；修复脚手架 TS 构建错误（vite/client 类型、@types/node、ol/Map 遮蔽、View.setView） |
