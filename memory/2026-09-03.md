# 2026-09-03 开发日志

## 今日变更

### 1. 删除一键启动脚本
- **文件**: `一键启动.bat`（已删除）
- **原因**: 脚本包含硬编码的本地路径 `D:\git\WebGIS-TEST`，其他组员无法直接使用
- **替代方案**: 改为手动启动说明，后端端口 3001，前端端口 8000（vite strictPort）

### 2. 更新作者信息
- **GitHub 用户名**: `mianwu27-ui`（原 `z985313021-alt`）
- **分支**: `feature/map-fix`
- **提交记录**: `57a6e12`

### 3. 新增用户注册功能
- **后端**: `server/scripts/user-db.mjs`（用户表 + scrypt 加盐哈希），`POST /api/auth/register`
- **前端**: `src/views/Register.vue` 注册页 + `src/data/api/auth.ts` + 路由 `/register` + 导航入口
- **验证**: 接口实测通过（成功/重名/重邮箱/格式错/密码短），`vue-tsc` 构建通过
- **提交记录**: `905352f`

### 4. 合并 dev 最新改动
- 拉取 `origin/dev`（8828d36：分类色 pin 标注等）合并进分支，无冲突，构建通过

## 注意事项
- 合并到 dev/main 必须走 Pull Request
- 不要直接 push 到受保护分支
- 用户数据库 `server/data/users.db` 已被 .gitignore 忽略，不会入库
